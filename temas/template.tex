% =============================================================
% Pandoc LaTeX Template — Queima Protocol
% Motor: XeLaTeX / LuaLaTeX  |  Variáveis de cor via YAML de tema
%
% Variáveis obrigatórias (definidas nos arquivos temas/*.yaml):
%   bgcolor, fgcolor, headingcolor, accentcolor,
%   linkcolor, codeblockbg, tableheadbg
% Variáveis opcionais: fontsize, mainfont, monofont, headingfont
% =============================================================

\documentclass[$if(fontsize)$$fontsize$,$endif$a4paper]{article}

% --- Codificação e Idioma ---
\usepackage{iftex}
\ifXeTeX
  \usepackage{fontspec}
\else\ifLuaTeX
  \usepackage{fontspec}
\else
  \usepackage[T1]{fontenc}
  \usepackage[utf8]{inputenc}
\fi\fi
\usepackage[portuguese]{babel}

% --- Cores ---
\usepackage[table]{xcolor}
\definecolor{PageBg}{HTML}{$bgcolor$}
\definecolor{BodyFg}{HTML}{$fgcolor$}
\definecolor{HeadingFg}{HTML}{$headingcolor$}
\definecolor{AccentFg}{HTML}{$accentcolor$}
\definecolor{LinkFg}{HTML}{$linkcolor$}
\definecolor{CodeBg}{HTML}{$codeblockbg$}
\definecolor{TableHeadBg}{HTML}{$tableheadbg$}

% --- Marca-texto (highlight) ---
\usepackage{soul}
$if(highlightcolor)$
\definecolor{HighlightBg}{HTML}{$highlightcolor$}
\sethlcolor{HighlightBg}
\newcommand{\queimahl}[1]{\hl{#1}}
$else$
\newcommand{\queimahl}[1]{\textbf{#1}}
$endif$

\pagecolor{PageBg}
\color{BodyFg}

% --- Geometria da PÃ¡gina ---
\usepackage[a4paper, top=2.5cm, bottom=2.5cm, left=3cm, right=2.5cm]{geometry}

% --- Ícones ---
\usepackage{fontawesome5}

% --- Microtipia e Espaçamento ---
\usepackage{microtype}
\usepackage{setspace}
\setstretch{1.3}
\setlength{\parskip}{0.6em}
\setlength{\parindent}{0pt}

% --- Fontes ---
$if(mainfont)$
\setmainfont{$mainfont$}
$else$
\usepackage{lmodern}
$endif$
$if(monofont)$
\setmonofont{$monofont$}[Scale=0.88]
$endif$
$if(headingfont)$
\newfontfamily\headingfontfamily{$headingfont$}
$endif$

% --- Section numbering ---
$if(numbersections)$
\setcounter{secnumdepth}{$if(secnumdepth)$$secnumdepth$$else$5$endif$}
$else$
\setcounter{secnumdepth}{-\maxdimen}
$endif$

% --- Títulos de Seção (com page break antes de cada capítulo) ---
\usepackage{titlesec}
\newcommand{\sectionbreak}{\clearpage}
\titleformat{\section}
  {\Large\bfseries\color{HeadingFg}$if(headingfont)$\headingfontfamily$endif$}
  {\textcolor{AccentFg}{\thesection}}{1em}{}
  [\vspace{-0.2em}\textcolor{AccentFg}{\rule{\linewidth}{0.8pt}}\vspace{0.2em}]
\titleformat{\subsection}
  {\large\bfseries\color{HeadingFg}$if(headingfont)$\headingfontfamily$endif$}
  {\textcolor{AccentFg}{\thesubsection}}{1em}{}
\titleformat{\subsubsection}
  {\normalsize\bfseries\color{AccentFg}$if(headingfont)$\headingfontfamily$endif$}
  {\thesubsubsection}{1em}{}

\titlespacing{\section}{0pt}{1.6em}{0.8em}
\titlespacing{\subsection}{0pt}{1.2em}{0.5em}
\titlespacing{\subsubsection}{0pt}{1em}{0.4em}

% --- Hiperlinks ---
\usepackage[colorlinks=true, breaklinks=true, bookmarks=true, bookmarksnumbered=true, bookmarksopen=true]{hyperref}
\hypersetup{
  linkcolor=LinkFg,
  urlcolor=LinkFg,
  citecolor=LinkFg,
  filecolor=LinkFg,
  pdftitle={$title$},
  pdfauthor={$author$},
  pdfsubject={Protocolo de Cutting Assistido},
  pdfkeywords={cutting, fat loss, protocolo, queima}
}

% --- Tabelas (com listras zebra) ---
\usepackage{longtable,booktabs,array}
\newcounter{none} % for unnumbered tables
\usepackage{calc} % for \real{} in column widths
\usepackage{multirow}
\usepackage{etoolbox}
\makeatletter
\patchcmd\longtable{\par}{\if@noskipsec\mbox{}\fi\par}{}{}
\makeatother
\IfFileExists{footnotehyper.sty}{\usepackage{footnotehyper}}{\usepackage{footnote}}
\makesavenoteenv{longtable}
\renewcommand{\arraystretch}{1.35}
\setlength{\LTpre}{1em}
\setlength{\LTpost}{1em}

% Zebra striping: alterna fundo das linhas para melhor legibilidade
\definecolor{ZebraOdd}{HTML}{$bgcolor$}
\colorlet{ZebraEven}{TableHeadBg!25!PageBg}
\rowcolors{2}{ZebraOdd}{ZebraEven}

% Cabeçalho de tabela colorido: injeta \rowcolor após \toprule
\let\oldtoprule\toprule
\renewcommand{\toprule}{\oldtoprule\rowcolor{TableHeadBg}}

% --- CitaÃ§Ãµes (Blockquotes) ---
\usepackage[framemethod=TikZ]{mdframed}
\renewenvironment{quote}{%
  \begin{mdframed}[
    topline=false, bottomline=false, rightline=false,
    linecolor=AccentFg, linewidth=3pt,
    backgroundcolor=CodeBg,
    fontcolor=BodyFg,
    innerleftmargin=14pt, innerrightmargin=10pt,
    innertopmargin=8pt, innerbottommargin=8pt,
    skipabove=0.8em, skipbelow=0.8em
  ]
  \small\itshape
}{%
  \end{mdframed}
}

% --- Blocos de CÃ³digo ---
\usepackage{fancyvrb}
\usepackage{fvextra}

\newenvironment{Shaded}{%
  \begin{mdframed}[
    linecolor=AccentFg, linewidth=0.5pt,
    backgroundcolor=CodeBg,
    innerleftmargin=10pt, innerrightmargin=10pt,
    innertopmargin=6pt, innerbottommargin=6pt,
    skipabove=0.5em, skipbelow=0.5em
  ]
}{%
  \end{mdframed}
}

\DefineVerbatimEnvironment{Highlighting}{Verbatim}{
  breaklines, breakanywhere,
  commandchars=\\\{\},
  fontsize=\small
}

\RecustomVerbatimEnvironment{verbatim}{Verbatim}{
  fontsize=\small, breaklines, breakanywhere
}

% --- Listas ---
\usepackage{enumitem}
\setlist[itemize,1]{label=\textcolor{AccentFg}{\textbullet}, leftmargin=*, itemsep=3pt, topsep=4pt}
\setlist[itemize,2]{label=\textcolor{AccentFg}{--}, leftmargin=*, itemsep=2pt}
\setlist[enumerate,1]{label=\textcolor{AccentFg}{\arabic*.}, leftmargin=*, itemsep=3pt, topsep=4pt}

% --- Imagens ---
\usepackage{graphicx}
\makeatletter
\def\maxwidth{\ifdim\Gin@nat@width>\linewidth\linewidth\else\Gin@nat@width\fi}
\makeatother
\setkeys{Gin}{width=\maxwidth, keepaspectratio}

% pandocbounded — exigido pelo Pandoc >=3.x para limitar imagens
\providecommand{\pandocbounded}[1]{%
  \begin{minipage}{\linewidth}%
    \centering #1%
  \end{minipage}%
}

% --- Cabeçalho e Rodapé ---
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{\small\color{AccentFg}\textit{$if(title)$$title$$endif$}}
\fancyhead[R]{\small\color{AccentFg}\textit{$if(version)$v$version$$endif$}}
\usepackage{lastpage}
\fancyfoot[C]{\small\color{AccentFg}\thepage\ / \pageref{LastPage}}
\renewcommand{\headrulewidth}{0.4pt}
\renewcommand{\headrule}{\hbox to\headwidth{\color{AccentFg}\leaders\hrule height \headrulewidth\hfill}}
\renewcommand{\footrulewidth}{0pt}
\fancypagestyle{plain}{\fancyhf{}\fancyfoot[C]{\small\color{AccentFg}\thepage\ / \pageref{LastPage}}\renewcommand{\headrulewidth}{0pt}}

% --- Sumário estilizado ---
\usepackage{tocloft}
\renewcommand{\cftsecfont}{\bfseries\color{HeadingFg}}
\renewcommand{\cftsecpagefont}{\bfseries\color{AccentFg}}
\renewcommand{\cftsubsecfont}{\color{BodyFg}}
\renewcommand{\cftsubsecpagefont}{\color{AccentFg}}
\renewcommand{\cftsubsubsecfont}{\small\color{BodyFg}}
\renewcommand{\cftsubsubsecpagefont}{\small\color{AccentFg}}
\renewcommand{\cftsecleader}{\color{AccentFg}\cftdotfill{\cftdotsep}}
\renewcommand{\cftsubsecleader}{\color{AccentFg!50}\cftdotfill{\cftdotsep}}
\setlength{\cftbeforesecskip}{0.6em}
\setlength{\cftbeforesubsecskip}{0.2em}

% --- Callout boxes (alertas, dicas, notas) ---
\newmdenv[%
  topline=false, bottomline=false, rightline=false,
  linecolor=red!70!black, linewidth=3pt,
  backgroundcolor=red!8!PageBg,
  fontcolor=BodyFg,
  innerleftmargin=14pt, innerrightmargin=10pt,
  innertopmargin=8pt, innerbottommargin=8pt,
  skipabove=0.8em, skipbelow=0.8em
]{queima@alertbox}
\newenvironment{queimaalert}[1][\textcolor{red!70!black}{\faExclamationTriangle\ ATENÇÃO}]{%
  \begin{queima@alertbox}\noindent\textbf{#1}\par\smallskip
}{\end{queima@alertbox}}

\newmdenv[%
  topline=false, bottomline=false, rightline=false,
  linecolor=AccentFg, linewidth=3pt,
  backgroundcolor=AccentFg!8!PageBg,
  fontcolor=BodyFg,
  innerleftmargin=14pt, innerrightmargin=10pt,
  innertopmargin=8pt, innerbottommargin=8pt,
  skipabove=0.8em, skipbelow=0.8em
]{queima@infobox}
\newenvironment{queimainfo}[1][\textcolor{AccentFg}{\faInfoCircle\ Nota}]{%
  \begin{queima@infobox}\noindent\textbf{#1}\par\smallskip
}{\end{queima@infobox}}

\newmdenv[%
  topline=false, bottomline=false, rightline=false,
  linecolor=green!50!black, linewidth=3pt,
  backgroundcolor=green!6!PageBg,
  fontcolor=BodyFg,
  innerleftmargin=14pt, innerrightmargin=10pt,
  innertopmargin=8pt, innerbottommargin=8pt,
  skipabove=0.8em, skipbelow=0.8em
]{queima@tipbox}
\newenvironment{queimatip}[1][\textcolor{green!50!black}{\faLightbulb\ Dica}]{%
  \begin{queima@tipbox}\noindent\textbf{#1}\par\smallskip
}{\end{queima@tipbox}}

% --- Separador visual (usado pelo filtro divider.lua) ---
\newcommand{\queimadivider}{%
  \par\vspace{0.6em}%
  \noindent\hfil{\textcolor{AccentFg}{\rule{0.5\linewidth}{0.8pt}}}\hfil%
  \par\vspace{0.6em}%
}

% --- Badge para referências [web:N] ---
\newcommand{\webref}[1]{%
  \textsuperscript{\small\color{AccentFg}[#1]}%
}

% --- Comandos Pandoc Obrigatórios ---
\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}

% Syntax highlighting tokens
\newcommand{\AlertTok}[1]{\textcolor{AccentFg}{\textbf{#1}}}
\newcommand{\AnnotationTok}[1]{\textcolor{HeadingFg}{\textit{#1}}}
\newcommand{\AttributeTok}[1]{\textcolor{LinkFg}{#1}}
\newcommand{\BaseNTok}[1]{#1}
\newcommand{\BuiltInTok}[1]{\textcolor{HeadingFg}{#1}}
\newcommand{\CharTok}[1]{\textcolor{AccentFg}{#1}}
\newcommand{\CommentTok}[1]{\textcolor{AccentFg}{\textit{#1}}}
\newcommand{\CommentVarTok}[1]{\textcolor{AccentFg}{\textit{#1}}}
\newcommand{\ConstantTok}[1]{\textcolor{AccentFg}{#1}}
\newcommand{\ControlFlowTok}[1]{\textcolor{HeadingFg}{\textbf{#1}}}
\newcommand{\DataTypeTok}[1]{\textcolor{HeadingFg}{#1}}
\newcommand{\DecValTok}[1]{\textcolor{AccentFg}{#1}}
\newcommand{\DocumentationTok}[1]{\textit{#1}}
\newcommand{\ErrorTok}[1]{\textcolor{AccentFg}{\textbf{#1}}}
\newcommand{\ExtensionTok}[1]{#1}
\newcommand{\FloatTok}[1]{\textcolor{AccentFg}{#1}}
\newcommand{\FunctionTok}[1]{\textcolor{LinkFg}{#1}}
\newcommand{\ImportTok}[1]{#1}
\newcommand{\InformationTok}[1]{\textcolor{HeadingFg}{\textit{#1}}}
\newcommand{\KeywordTok}[1]{\textcolor{HeadingFg}{\textbf{#1}}}
\newcommand{\NormalTok}[1]{#1}
\newcommand{\OperatorTok}[1]{\textcolor{AccentFg}{#1}}
\newcommand{\OtherTok}[1]{#1}
\newcommand{\PreprocessorTok}[1]{\textcolor{AccentFg}{\textit{#1}}}
\newcommand{\RegionMarkerTok}[1]{#1}
\newcommand{\SpecialCharTok}[1]{\textcolor{AccentFg}{#1}}
\newcommand{\SpecialStringTok}[1]{\textcolor{AccentFg}{#1}}
\newcommand{\StringTok}[1]{\textcolor{AccentFg}{#1}}
\newcommand{\VariableTok}[1]{#1}
\newcommand{\VerbatimStringTok}[1]{\textcolor{AccentFg}{#1}}
\newcommand{\WarningTok}[1]{\textcolor{AccentFg}{\textbf{\textit{#1}}}}

% =============================================================
\begin{document}

% ---- Página de Título ----
$if(title)$
\begin{titlepage}
  \centering
  \vspace*{2cm}
  {\textcolor{AccentFg}{\rule{\linewidth}{2pt}}\par}
  \vspace{1.2cm}
  {\color{AccentFg}\fontsize{48}{52}\selectfont\faFire\par}
  \vspace{0.8cm}
  {\Huge\bfseries\color{HeadingFg}$if(headingfont)$\headingfontfamily$endif$ $title$ \par}
  \vspace{0.6cm}
  $if(subtitle)$
    {\Large\color{AccentFg}\itshape $subtitle$ \par}
    \vspace{0.3cm}
  $endif$
  {\textcolor{AccentFg}{\rule{\linewidth}{0.5pt}}\par}
  \vspace{1.8cm}
  $if(author)${\large\color{BodyFg}\faUser\ \ $author$ \par}$endif$
  \vspace{0.3cm}
  $if(date)${\normalsize\color{BodyFg}\faCalendar\ \ $date$ \par}$endif$
  \vfill
  {\small\color{AccentFg}\faTags\ \ Versão\ $if(version)$$version$$else$1.0.0$endif$ \par}
  \vspace{0.8cm}
  {\textcolor{AccentFg}{\rule{\linewidth}{0.5pt}}\par}
\end{titlepage}
\newpage
$endif$

% ---- SumÃ¡rio ----
$if(toc)$
{
  \hypersetup{linkcolor=BodyFg}
  \tableofcontents
}
\newpage
$endif$

% ---- ConteÃºdo ----
$body$

\end{document}

